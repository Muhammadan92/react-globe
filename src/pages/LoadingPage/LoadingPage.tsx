import Flex from 'jcicl/Flex';
import Loading from 'jcicl/Loading';

const ContactsPage = () => {
  return (
    <Flex column width="100%" alignItems="center">
      <Loading />
    </Flex>
  );
};

export default ContactsPage;
